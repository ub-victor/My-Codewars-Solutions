function failStep(){
    return new Promise((resolve, reject) => {
        reject("Water dinna boil");// simulating a failure
    });
}


async function testFail(){
    try {
        const result = await failStep();
        console.log(result);
    } catch (error) {
        console.error("Caught an error:", error);
    }
}